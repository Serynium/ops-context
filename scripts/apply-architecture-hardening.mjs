import { readFile, writeFile } from "node:fs/promises"

const patches = [{"path":"worker/src/services.ts","old":"import {\n  d1FailureTelemetry,\n  d1SuccessTelemetry,\n  type DatabaseOperation\n} from \"./database-observability.js\"\n","new":"import {\n  classifyD1SuccessTelemetry,\n  d1FailureTelemetry,\n  d1SuccessTelemetry,\n  type DatabaseOperation\n} from \"./database-observability.js\"\n"},{"path":"worker/src/services.ts","old":"Effect.tap((result) =>\n  Effect.forEach(successTelemetry(result), (telemetry) =>\n    Effect.all([\n      Effect.annotateCurrentSpan(telemetry),\n      Effect.logInfo(telemetry)\n    ], { discard: true }), { discard: true })\n),\n","new":"Effect.tap((result) =>\n  Effect.forEach(successTelemetry(result), (telemetry) => {\n    const decision = classifyD1SuccessTelemetry(telemetry)\n    const log = decision === \"warning\"\n      ? Effect.logWarning(telemetry)\n      : decision === \"info\"\n        ? Effect.logInfo(telemetry)\n        : Effect.void\n    return Effect.all([\n      Effect.annotateCurrentSpan(telemetry),\n      log\n    ], { discard: true })\n  }, { discard: true })\n),\n"},{"path":"worker/src/services.ts","old":"readonly appHost: string\nreadonly mcpHost?: string\nreadonly accessAppAudience?: string\n","new":"readonly appHost: string\nreadonly mcpHost?: string\nreadonly accessTeamDomain?: string\nreadonly accessAppAudience?: string\n"},{"path":"worker/src/services.ts","old":"const mcpHost = env.OPS_MCP_HOST?.trim().toLowerCase()\nconst accessAppAudience = env.OPS_ACCESS_APP_AUD?.trim()\n","new":"const mcpHost = env.OPS_MCP_HOST?.trim().toLowerCase()\nconst accessTeamDomain = env.OPS_ACCESS_TEAM_DOMAIN?.trim().toLowerCase()\nconst accessAppAudience = env.OPS_ACCESS_APP_AUD?.trim()\n"},{"path":"worker/src/services.ts","old":"...(mcpHost ? { mcpHost } : {}),\n...(accessAppAudience ? { accessAppAudience } : {}),\n","new":"...(mcpHost ? { mcpHost } : {}),\n...(accessTeamDomain ? { accessTeamDomain } : {}),\n...(accessAppAudience ? { accessAppAudience } : {}),\n"},{"path":"worker/src/services.ts","old":"maxPushAttempts: Math.min(20, Math.max(1, Number.parseInt(env.OPS_PUSH_MAX_ATTEMPTS ?? \"6\", 10) || 6)),\n","new":"// The primary Queue permits five retries after the first delivery.\nmaxPushAttempts: Math.min(6, Math.max(1, Number.parseInt(env.OPS_PUSH_MAX_ATTEMPTS ?? \"6\", 10) || 6)),\n"},{"path":"worker/src/types.ts","old":"readonly OPS_APP_HOST?: string\nreadonly OPS_MCP_HOST?: string\nreadonly OPS_ACCESS_APP_AUD?: string\n","new":"readonly OPS_APP_HOST?: string\nreadonly OPS_MCP_HOST?: string\nreadonly OPS_ACCESS_TEAM_DOMAIN?: string\nreadonly OPS_ACCESS_APP_AUD?: string\n"},{"path":"worker/src/events.ts","old":"encodedQueueCommandBytes,\nQUEUE_COMMAND_MAX_BYTES,\nQUEUE_COMMAND_VERSION,\n","new":"encodedQueueCommandBytes,\nqueueBillingChunks,\nQUEUE_BILLING_CHUNK_BYTES,\nQUEUE_COMMAND_MAX_BYTES,\nQUEUE_COMMAND_VERSION,\n"},{"path":"worker/src/events.ts","old":"export interface EventAccepted {\n  readonly id: string\n  readonly accepted_at: string\n  readonly status: \"queued\"\n}\n","new":"export interface EventAccepted {\n  readonly id: string\n  readonly accepted_at: string\n  readonly status: \"queued\"\n}\n\nexport type IngestDeadLetterOutcome =\n  | { readonly _tag: \"Recovered\" }\n  | { readonly _tag: \"Terminalized\" }\n"},{"path":"worker/src/events.ts","old":"if (encodedQueueCommandBytes(command) > QUEUE_COMMAND_MAX_BYTES) {\n  return yield* Effect.fail(invalidEvent(\"event is too large for durable Queue acceptance\"))\n}\nyield* queue.send(command)\n","new":"const commandBytes = encodedQueueCommandBytes(command)\nconst billingChunks = queueBillingChunks(commandBytes)\nyield* Effect.annotateCurrentSpan({\n  \"queue.command\": command._tag,\n  \"queue.command.bytes\": commandBytes,\n  \"queue.command.billing_chunks\": billingChunks\n})\nif (commandBytes > QUEUE_BILLING_CHUNK_BYTES) {\n  yield* Effect.logWarning({\n    event: \"queue.command.large\",\n    command: command._tag,\n    event_id: eventId,\n    bytes: commandBytes,\n    billing_chunks: billingChunks,\n    billing_chunk_bytes: QUEUE_BILLING_CHUNK_BYTES\n  })\n}\nif (commandBytes > QUEUE_COMMAND_MAX_BYTES) {\n  return yield* Effect.fail(invalidEvent(\"event is too large for durable Queue acceptance\"))\n}\nyield* queue.send(command)\n"},{"path":"worker/src/events.ts","old":"for (const subscriptionId of pending) {\n  yield* queue.send({\n    _tag: \"DeliverPush\",\n    version: QUEUE_COMMAND_VERSION,\n    eventId,\n    subscriptionId\n  })\n  const queuedAt = nowIso()\n  yield* events.markPushJobQueued(eventId, subscriptionId, queuedAt)\n}\n","new":"for (const subscriptionId of pending) {\n  yield* queue.send({\n    _tag: \"DeliverPush\",\n    version: QUEUE_COMMAND_VERSION,\n    eventId,\n    subscriptionId\n  })\n}\n// One event-level marker replaces one D1 update per destination. If the\n// consumer crashes before this write, replay may republish messages; the\n// delivery claim makes those duplicates harmless.\nyield* events.markFanoutPublished(eventId, nowIso())\n"},{"path":"worker/src/events.ts","old":"export const processIngestDeadLetter = (\n  command: IngestEventCommand\n): Effect.Effect<void, EventError, ProjectsRepository | EventsRepository | SettingsRepository | SilencesRepository | SubscriptionsRepository | PushQueue | AppConfig> =>\n  processIngestEvent(command).pipe(\n    Effect.catch((failure) =>\n      Effect.gen(function*() {\n        const events = yield* EventsRepository\n        const failedAt = nowIso()\n        const reason = `ingestion exhausted Queue retries: ${failure.message}`.slice(0, 4_000)\n        yield* events.recordIngestionFailure({\n          eventId: command.eventId,\n          projectId: command.projectId,\n          externalId: command.event.external_id ?? null,\n          reason,\n          failedAt\n        })\n      })\n    )\n  ).pipe(Effect.withSpan(\"EventIngestion.deadLetter\", { attributes: { eventId: command.eventId } }))\n","new":"export const processIngestDeadLetter = (\n  command: IngestEventCommand\n): Effect.Effect<IngestDeadLetterOutcome, EventError, ProjectsRepository | EventsRepository | SettingsRepository | SilencesRepository | SubscriptionsRepository | PushQueue | AppConfig> =>\n  processIngestEvent(command).pipe(\n    Effect.as({ _tag: \"Recovered\" } as const),\n    Effect.catch((failure) =>\n      Effect.gen(function*() {\n        const events = yield* EventsRepository\n        const failedAt = nowIso()\n        const reason = `ingestion exhausted Queue retries: ${failure.message}`.slice(0, 4_000)\n        yield* events.recordIngestionFailure({\n          eventId: command.eventId,\n          projectId: command.projectId,\n          externalId: command.event.external_id ?? null,\n          reason,\n          failedAt\n        })\n        return { _tag: \"Terminalized\" } as const\n      })\n    )\n  ).pipe(Effect.withSpan(\"EventIngestion.deadLetter\", { attributes: { eventId: command.eventId } }))\n"},{"path":"worker/src/application.ts","old":"type EventError,\ntype ListEventsInput\n","new":"type EventError,\ntype IngestDeadLetterOutcome,\ntype ListEventsInput\n"},{"path":"worker/src/application.ts","old":"readonly process: (message: IngestEventCommand) => Effect.Effect<void, EventError>\nreadonly deadLetter: (message: IngestEventCommand) => Effect.Effect<void, EventError>\n","new":"readonly process: (message: IngestEventCommand) => Effect.Effect<void, EventError>\nreadonly deadLetter: (message: IngestEventCommand) => Effect.Effect<IngestDeadLetterOutcome, EventError>\n"},{"path":"worker/src/repositories.ts","old":"readonly listPendingSubscriptionIds: (eventId: string) => Effect.Effect<ReadonlyArray<string>, RepositoryUnavailable>\nreadonly markPushJobQueued: (eventId: string, subscriptionId: string, queuedAt: string) => Effect.Effect<void, RepositoryUnavailable>\n","new":"readonly listPendingSubscriptionIds: (eventId: string) => Effect.Effect<ReadonlyArray<string>, RepositoryUnavailable>\nreadonly markFanoutPublished: (eventId: string, publishedAt: string) => Effect.Effect<void, RepositoryUnavailable>\n"},{"path":"worker/src/repositories.ts","old":"readonly pruneBefore: (cutoff: string) => Effect.Effect<number, RepositoryUnavailable>\n","new":"readonly pruneBefore: (cutoff: string, limit: number) => Effect.Effect<number, RepositoryUnavailable>\n"},{"path":"worker/src/repositories.ts","old":"listPendingSubscriptionIds: (eventId) => db.all(\n  Schema.Struct({ subscription_id: Schema.String }),\n  \"SELECT subscription_id FROM push_jobs WHERE event_id = ? AND state = 'pending' ORDER BY subscription_id\",\n  [eventId], \"push_jobs.list_pending_for_publication\"\n).pipe(Effect.map((rows) => rows.map((row) => row.subscription_id))),\nmarkPushJobQueued: (eventId, subscriptionId, queuedAt) => db.run(\n  `UPDATE push_jobs SET state = 'queued', queued_at = ?, updated_at = ?\n   WHERE event_id = ? AND subscription_id = ? AND state = 'pending'`,\n  [queuedAt, queuedAt, eventId, subscriptionId], \"push_jobs.mark_queued\"\n).pipe(Effect.asVoid),\n","new":"listPendingSubscriptionIds: (eventId) => db.all(\n  Schema.Struct({ subscription_id: Schema.String }),\n  `SELECT j.subscription_id\n   FROM push_jobs j\n   JOIN events e ON e.id = j.event_id\n   WHERE j.event_id = ? AND j.state = 'pending'\n     AND e.fanout_published_at IS NULL\n   ORDER BY j.subscription_id`,\n  [eventId], \"push_jobs.list_pending_for_publication\"\n).pipe(Effect.map((rows) => rows.map((row) => row.subscription_id))),\nmarkFanoutPublished: (eventId, publishedAt) => db.run(\n  `UPDATE events SET fanout_published_at = ?\n   WHERE id = ? AND fanout_published_at IS NULL`,\n  [publishedAt, eventId], \"events.mark_fanout_published\"\n).pipe(Effect.asVoid),\n"},{"path":"worker/src/repositories.ts","old":"{ sql: \"UPDATE events SET silence_id = NULL WHERE id = ?\", params: [eventId] },\n","new":"{\n  sql: \"UPDATE events SET silence_id = NULL, fanout_published_at = NULL WHERE id = ?\",\n  params: [eventId]\n},\n"},{"path":"worker/src/repositories.ts","old":"pruneBefore: (cutoff) => db.runCounted(\n  \"DELETE FROM events WHERE created_at < ?\",\n  [cutoff],\n  \"events.prune\"\n),\n","new":"pruneBefore: (cutoff, limit) => db.runCounted(\n  `DELETE FROM events\n   WHERE id IN (\n     SELECT id FROM events\n     WHERE created_at < ?\n     ORDER BY created_at ASC, id ASC\n     LIMIT ?\n   )`,\n  [cutoff, limit],\n  \"events.prune\"\n),\n"},{"path":"worker/test/queue-first-ingestion.integration.test.ts","old":"encodedQueueCommandBytes,\nQUEUE_COMMAND_MAX_BYTES,\nQUEUE_COMMAND_VERSION,\n","new":"encodedQueueCommandBytes,\nqueueBillingChunks,\nQUEUE_BILLING_CHUNK_BYTES,\nQUEUE_COMMAND_MAX_BYTES,\nQUEUE_COMMAND_VERSION,\n"},{"path":"worker/test/queue-first-ingestion.integration.test.ts","old":"expect(afterCrash.results).toEqual([\n  { state: \"pending\", count: 2 },\n  { state: \"queued\", count: 1 }\n])\n","new":"expect(afterCrash.results).toEqual([\n  { state: \"pending\", count: 3 }\n])\n"},{"path":"worker/test/queue-first-ingestion.integration.test.ts","old":"expect(recovered).toHaveLength(2)\nconst finalJobs = await env.DB.prepare(\n  \"SELECT state, COUNT(*) AS count FROM push_jobs GROUP BY state\"\n).all<{ state: string; count: number }>()\nexpect(finalJobs.results).toEqual([{ state: \"queued\", count: 3 }])\n","new":"// The already-published destination may be republished after a crash. The\n// delivery consumer's conditional claim makes the duplicate harmless.\nexpect(recovered).toHaveLength(3)\nconst finalJobs = await env.DB.prepare(\n  \"SELECT state, COUNT(*) AS count FROM push_jobs GROUP BY state\"\n).all<{ state: string; count: number }>()\nexpect(finalJobs.results).toEqual([{ state: \"pending\", count: 3 }])\nconst fanout = await env.DB.prepare(\n  \"SELECT fanout_published_at FROM events WHERE id = ?\"\n).bind(command().eventId).first<{ fanout_published_at: string | null }>()\nexpect(fanout?.fanout_published_at).not.toBeNull()\n"},{"path":"worker/test/queue-first-ingestion.integration.test.ts","old":"expect(accepted).toHaveLength(1)\nexpect(encodedQueueCommandBytes(accepted[0]!)).toBeLessThanOrEqual(QUEUE_COMMAND_MAX_BYTES)\n","new":"expect(accepted).toHaveLength(1)\nconst bytes = encodedQueueCommandBytes(accepted[0]!)\nexpect(bytes).toBeGreaterThan(QUEUE_BILLING_CHUNK_BYTES)\nexpect(bytes).toBeLessThanOrEqual(QUEUE_COMMAND_MAX_BYTES)\nexpect(queueBillingChunks(bytes)).toBe(2)\n"},{"path":"worker/test/queue-first-ingestion.integration.test.ts","old":"const failed = ingestionLayer(() => Effect.fail(queueUnavailable(\"downstream Queue unavailable\")))\nawait Effect.runPromise(processIngestDeadLetter(command()).pipe(Effect.provide(failed)))\n\nconst failure = await env.DB.prepare(\n","new":"const failed = ingestionLayer(() => Effect.fail(queueUnavailable(\"downstream Queue unavailable\")))\nconst outcome = await Effect.runPromise(\n  processIngestDeadLetter(command()).pipe(Effect.provide(failed))\n)\nexpect(outcome).toEqual({ _tag: \"Terminalized\" })\n\nconst failure = await env.DB.prepare(\n"},{"path":"worker/test/queue-first-ingestion.integration.test.ts","old":"expect(pending?.count).toBe(0)\n","new":"expect(pending?.count).toBe(3)\nconst fanout = await env.DB.prepare(\n  \"SELECT fanout_published_at FROM events WHERE id = 'evt_silenced'\"\n).first<{ fanout_published_at: string | null }>()\nexpect(fanout?.fanout_published_at).not.toBeNull()\n"},{"path":"worker/test/database-observability.test.ts","old":"import {\n  D1StructuredLoggerLive,\n  d1SuccessTelemetry\n} from \"../src/database-observability.js\"\n","new":"import {\n  D1StructuredLoggerLive,\n  classifyD1SuccessTelemetry,\n  d1SuccessTelemetry\n} from \"../src/database-observability.js\"\n"},{"path":"worker/test/database-observability.test.ts","old":"it(\"records remote serving location without inventing local values\", () => {\n","new":"it(\"samples routine successes but always logs expensive queries\", () => {\n  const routine = d1SuccessTelemetry(\n    \"projects.get_by_id\",\n    \"query\",\n    result([{ id: \"prj_1\" }], { rows_read: 1, rows_written: 0, duration: 1 })\n  )\n  expect(classifyD1SuccessTelemetry(routine, 0.99)).toBe(\"none\")\n  expect(classifyD1SuccessTelemetry(routine, 0)).toBe(\"info\")\n\n  const expensive = d1SuccessTelemetry(\n    \"events.list\",\n    \"query\",\n    result([], { rows_read: 2_000, rows_written: 0, duration: 5 })\n  )\n  expect(classifyD1SuccessTelemetry(expensive, 0.99)).toBe(\"warning\")\n})\n\nit(\"records remote serving location without inventing local values\", () => {\n"},{"path":"worker/test/database-observability.test.ts","old":"all: () => Promise.resolve(result([{ id: \"evt_1\" }]))\n","new":"all: () => Promise.resolve(result(\n  [{ id: \"evt_1\" }],\n  { rows_read: 1_000 }\n))\n"},{"path":"worker/test/database-observability.test.ts","old":"all: () => Promise.resolve(result([{ id: \"evt_1\" }]))\n","new":"all: () => Promise.resolve(result(\n  [{ id: \"evt_1\" }],\n  { rows_read: 1_000 }\n))\n"},{"path":"worker/test/database-observability.test.ts","old":"const consoleLog = vi.spyOn(console, \"log\").mockImplementation(() => undefined)\n","new":"const consoleWarn = vi.spyOn(console, \"warn\").mockImplementation(() => undefined)\n"},{"path":"worker/test/database-observability.test.ts","old":"expect(consoleLog).toHaveBeenCalledWith(expect.objectContaining({\n","new":"expect(consoleWarn).toHaveBeenCalledWith(expect.objectContaining({\n"},{"path":"worker/test/database-observability.test.ts","old":"consoleLog.mockRestore()\n","new":"consoleWarn.mockRestore()\n"}]
const read = (path) => readFile(path, "utf8")
for (const patch of patches) {
  const content = await read(patch.path)
  if (content.includes(patch.old)) {
    await writeFile(patch.path, content.replace(patch.old, patch.new))
  } else if (!content.includes(patch.new)) {
    throw new Error(`Missing expected text in ${patch.path}`)
  }
}

const packageJson = JSON.parse(await read("package.json"))
packageJson.dependencies = { ...packageJson.dependencies, jose: "6.2.5" }
packageJson.scripts = {
  ...packageJson.scripts,
  "check:migrations": "node scripts/check-migration-prefixes.mjs",
  "build:worker:no-cron": "wrangler deploy --dry-run --config wrangler.no-cron.jsonc --outdir .worker-dist-no-cron",
  "deploy:no-cron": "pnpm build:web && wrangler deploy --config wrangler.no-cron.jsonc",
  check: "pnpm check:migrations && pnpm typecheck && pnpm test && pnpm build:worker && pnpm build:worker:no-cron"
}
await writeFile("package.json", `${JSON.stringify(packageJson, null, 2)}\n`)

const adr2Path = "docs/decisions/0002-retain-single-worker-deployment.md"
const adr2 = await read(adr2Path)
const amendment = `\n## 2026-08-31 hardening amendment\n\nThe repository now has Workers-runtime D1/Queue integration tests, decoded repository ports, tagged protocol-independent errors, Queue-first ingestion, FTS, and a grouped-event read model.\n\nThe Static Assets router does not always forward \`ctx.access\`. This is not a reason to split the deployment: the Worker now verifies the forwarded Access JWT against Cloudflare's JWKS when the direct runtime context is absent. The single-Worker decision remains accepted. Reconsider it only when production measurements or security policy require an independent deployment boundary.\n`
if (!adr2.includes("2026-08-31 hardening amendment")) await writeFile(adr2Path, `${adr2.trimEnd()}${amendment}\n`)

const readme = await read("README.md")
if (!readme.includes("docs/operations-hardening.md")) {
  await writeFile("README.md", `${readme.trimEnd()}\n\n## Architecture and operations\n\nThe production security, Queue/D1 cost model, DLQ alerts, no-Cron deployment, and smoke-test checklist are documented in [\`docs/operations-hardening.md\`](docs/operations-hardening.md).\n`)
}
